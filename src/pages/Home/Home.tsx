import React, { FC, ReactElement } from 'react';
import './Home.css';
import { Hero } from '../../components';

const Home: FC = (): ReactElement => {
  return (
    <>
      <Hero />
    </>
  );
};

export default Home;
